services:
  - type: web
    name: motomanutencao
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn start_server:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0